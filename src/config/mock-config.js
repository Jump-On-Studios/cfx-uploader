/**
 * Mock configuration used by the orchestrator.
 * The orchestrator clones this object into a local `config` variable.
 */
module.exports = {
  portalName: 'jo_chest 2',
  githubRepository: 'Jump-On-Studios/RedM-jo_chest',
  foldersToZip: ['jo_chest'],
};
